# Tasks

- [x] Update `src/app/globals.css`
    - [x] Change `@page` margin to `0` to hide browser metadata
    - [x] Add padding to `body` or main container in `@media print` to restore content layout safety zone
- [ ] Verify print preview in browser (using emulation or actual print preview)
